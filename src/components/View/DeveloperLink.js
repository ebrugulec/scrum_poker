import React from 'react'

const DeveloperLink = (props) => {
    return (
        <Button
            className={className}
            onClick={onClick}
            type={type}>
            {text}
        </Button>
    )
}

export default DeveloperLink